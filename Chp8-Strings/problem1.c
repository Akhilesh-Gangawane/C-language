#include <stdio.h>
#include <string.h>

int main(){
    char str[3];
    scanf("%s", str);
    printf("%s", str);
    // scanf("%c", &str);
    // printf("%c", str); /* this can happen only in for loop */
    return 0;
}