#include <stdio.h>

int main(){
    char name[4];
    gets(name);
    puts(name); // it is same as printf but the difference is it add \n and transfer the curser to next line
    printf("Intell");
    return 0;
}