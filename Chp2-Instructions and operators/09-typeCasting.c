//to change the type of variable

#include<stdio.h>

int main(){
    int n=54;
    float m=874.48574;

    n=(int) m; //conversion of value of data type 
    printf("the changed value of n is %d", n);
    return 0;
}