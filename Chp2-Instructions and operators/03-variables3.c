//assignig same value for all variables

#include<stdio.h>

int main(){
    int a, b, c, d, e;
    a=b=c=d=e=30;
    printf("the value of all varibles is same and that is %d", a);
    return 0;
}