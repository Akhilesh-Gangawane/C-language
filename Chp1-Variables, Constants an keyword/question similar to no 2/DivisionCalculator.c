#include <stdio.h>

int main()
{
    int a, b;
    printf("the value of divisor is ");
    scanf("%d", &a);
    printf("the value of dividened is ");
    scanf("%d", &b);

    printf("the value on dividing the the number is %d", a / b);

    return 0;
}