#include <stdio.h>

int main()
{
    int a, b;
    printf("the value of first number is ");
    scanf("%d", &a);
    printf("the value of second number is ");
    scanf("%d", &b);

    printf("the value of product of these two number is %d", a * b);

    return 0;
}