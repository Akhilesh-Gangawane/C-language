#include <stdio.h>

int main()
{
    float r;
    printf("the value of radius of circle is\n ");
    scanf("%f", &r);

    printf("the value of area of circle is %f", 3.145 * (r * r));
    return 0;
}