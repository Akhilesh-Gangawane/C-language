#include <stdio.h>

int main()
{
    float a, b;

    printf("the value of the lenght of the rectangle is ");
    scanf("%f", &a);

    printf("the value of the breath of the rectangle is ");
    scanf("%f", &b);

    printf("the value of area of the rectangle is %f", a * b);
    return 0;
}