#include<stdio.h>

int main(){
    int a; //Declaration of variable
    a = 6; //variable initialization
    printf("the value is %d",a); //%d is a int wala c being a old language it needs to be specified
    return 0;
}